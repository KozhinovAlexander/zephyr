common:
  build_only: true
  tags:
    - drivers
    - opamp
tests:
  drivers.build_all.opamp.stm32_opamp.opamp1:
    extra_args:
      - DTC_OVERLAY_FILE="stm32_opamp/opamp1.dts"
    filter: dt_compat_enabled('st,stm32-opamp')
